---
import {
  CodeXml,
  SwatchBook,
  Braces,
  Rocket,
  PanelsTopLeft,
  LaptopMinimal,
} from "@lucide/astro";
---

<section>
  <h3 id="tools">Tools</h3>
  <div class="container">
    <div class="cards">
      <div class="card-header">
        <CodeXml size={24} color="var(--accent-red)" />
        <h4>HTML</h4>
      </div>
      Semantic and accessible web page structuring. Experience with HTML5, semantic
      tags, basic SEO and markup best practices to create solid and scalable foundations.
    </div>

    <div class="cards">
      <div class="card-header">
        <SwatchBook size={24} color="var(--accent-blue)" />
        <h4>CSS</h4>
      </div>
      Modern and responsive styling. Mastery of Flexbox, animations, transitions
      and media queries. Focused on creating elegant interfaces that work perfectly
      on any device.
    </div>

    <div class="cards">
      <div class="card-header">
        <Braces size={24} color="var(--accent-lime)" />
        <h4>JavaScript</h4>
      </div>
      Client-side interactivity and logic development. DOM manipulation, events,
      fetch API and ES6+. Building dynamic and functional user experiences.
    </div>

    <div class="cards">
      <div class="card-header">
        <Rocket size={24} color="var(--accent-magenta)" />
        <h4>Astro</h4>
      </div>
      Modern framework for ultra-fast websites. Experience with components, routing
      and performance optimization. Ideal for creating static and dynamic sites with
      excellent DX.
    </div>

    <div class="cards">
      <div class="card-header">
        <PanelsTopLeft size={24} color="var(--accent-mint)" />
        <h4>Web Design</h4>
      </div>
      Intuitive and attractive interface design. Focus on UX/UI, typography, color
      palettes and modern layouts. Creating memorable visual experiences.
    </div>

    <div class="cards">
      <div class="card-header">
        <LaptopMinimal size={24} color="var(--accent-orange)" />
        <h4>Advanced Linux</h4>
      </div>
      Linux System Administration. Shell scripting, process management, and system
      optimization. Focus on task automation in terminal environments.
    </div>
  </div>
</section>

<style>
  section {
    width: 100%;
    min-height: 100vh;
    padding: 1vh 10vw;
    display: flex;
    flex-direction: column;
    justify-content: center;
    text-align: left;
    box-sizing: border-box;
  }

  h3 {
    font-size: 40px;
    text-align: center;
  }

  .container {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    margin-left: auto;
    margin-right: auto;
  }
  .cards {
    background-color: var(--bg-1);
    border: 1px solid var(--border-color);
    border-radius: 15px;
    padding: 30px;
    text-align: center;
    margin: 10px;
    flex: auto;
    max-width: 300px;
    transition: 0.3s ease-in-out;
    font-size: 20px;
  }

  .cards:hover {
    background-color: var(--bg-2);
    transform: translateY(-10px);
  }

  .card-header {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 10px;
  }

  @media (max-width: 800px) {
    section {
      padding: 5vh 5vw;
    }

    h3 {
      font-size: 30px;
    }

    .cards:hover {
      background-color: var(--bg-1);
      transform: translateY(0px);
    }
  }
</style>
